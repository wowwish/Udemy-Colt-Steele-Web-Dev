<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>

<body>
    <h1>Edit Product</h1>
    <!-- USING METHOD OVERRIDE THROUGH QUERY STRING -->
    <form action="/products/<%=product._id%>?_method=PUT" method="POST">
        <label for="name">Product Name:</label>
        <input id="name" type="text" name="name" placeholder="product name" value="<%=product.name%>">
        <label for="price">Price (Unit):</label>
        <input id="price" type="number" name="price" placeholder="price" step="0.01" value="<%=product.price%>">
        <!-- The step attribute above can be tweaked to allow any number of decimal places. 
            Sticking to 0.01 for currencies is recommended. -->
        <label for="category"></label>
        <select name="category" id="category">
            <!-- The selected attribute will be added to the respective category in the selector using 
                ternary operator and ejs templating -->
            <% for (let category of categories) { %>
                <option value="<%=category%>" <%=product.category===category ? 'selected' : '' %>>
                    <%=category%>
                </option>
                <% } %>
        </select>
        <button>Submit</button>
    </form>
    <a href="/products/<%=product._id%>">Cancel</a>
</body>

</html>